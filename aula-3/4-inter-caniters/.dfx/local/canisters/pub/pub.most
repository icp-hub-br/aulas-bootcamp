// Version: 1.0.0
type Counter = {topic : Text; value : Nat};
type List<T> = ?(T, List<T>);
type Subscriber = {callback : shared Counter -> (); topic : Text};
actor {
  stable var subscribers : ?(Subscriber, List<Subscriber>)
};
